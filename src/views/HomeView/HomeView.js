import s from './HomeView.module.css';

const HomeView = () => (
  <div className={s.bg}>
    <h1 className={s.title}>Welcome to the app, that stores your contacts </h1>
  </div>
);

export default HomeView;
